<div class="container">
	<h1>Termos para revenda</h1>

	<div>
		<%= @store.terms %>
	</div>

	<div class="d-flex justify-content-between my-3">
		<%= link_to 'Voltar', @store, class: 'btn btn-outline-primary' %>
		<%= link_to 'Confirmar', store_subscribe_to_store_path(@store), method: :post, class: 'btn btn-primary' %>
	</div>
</div>